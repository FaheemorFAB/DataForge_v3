services:
  web:
    build:
      context: .
      dockerfile: services/web/Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./services/web/instance:/app/services/web/instance
